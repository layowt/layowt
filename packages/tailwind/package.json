{
  "name": "@layowt/tailwind-config",
  "version": "1.0.0",
  "description": "Tailwind CSS configuration for Layowt",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
