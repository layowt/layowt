'use client';
import { Dialog, DialogContent } from '@/components/ui/dialog';

export default function UserSiteData() {
  return (
    <Dialog
      modal={true}
      defaultOpen={true}
    >
      Testing
    </Dialog>
  );
}
