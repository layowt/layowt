/**
 * This function is used to set primitive values with 
 * different keys
 * 
 * e.g. - 'innerHTML': string
 */

/**
 * Used to set the innerHTML value of a component
 */
export type innerHTML = string

/**
 * Used to declare timestamps
 */
export type ISOString = string